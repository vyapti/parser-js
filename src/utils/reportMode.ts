/**
 * ReportMode Enumeration
 *
 * An enumeration to hold the type of report that should be generated
 */
enum ReportMode {
  Simple = 'simple',
  Detail = 'detail',
  Tree = 'tree',
}

export default ReportMode;
